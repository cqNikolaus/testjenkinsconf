credentials:
  system:
    domainCredentials:
    - credentials:
      - string:
          scope: GLOBAL
          id: "diffblue-license-key"
          secret: ${DIFFBLUE_LICENSE_KEY}
          description: "Diffblue license key"
